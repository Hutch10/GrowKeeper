export class AuditSentinel {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  static validateTransition(_from: string, _to: string, _context: string): void {
    // stub — state machine validation was removed with geofencing
  }
}
